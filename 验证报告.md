# 语音输入插件 · 验证报告

> 依据《语音输入插件·创造模式提示词.md》的验证要求。能力检测随插件运行实时显示在
> 设置 → 语音输入 页面；以下手动测试清单需在真实浏览器 + 麦克风环境下逐项执行。

## 一、环境自检（能力检测报告）

| 项目 | 结果 | 说明 |
|---|---|---|
| SpeechRecognition 支持 | ✔ / ✘ | Chrome / Edge 均支持 `webkitSpeechRecognition` |
| 安全上下文（https/localhost） | ✔ | 本 GUI 为 `http://127.0.0.1:3080` |
| 麦克风设备 | ✔ / ✘ / 检测中 | 由 `navigator.mediaDevices.enumerateDevices()` 实时检测 |
| 插件激活（voice-2/pkg-4） | ✔ | `state: running`，三个 Slot 均挂载 |
| 麦克风按钮注入 | ✔ | `conversation.input.right` → `voice-input-mic`（active） |
| 预览条注入 | ✔ | `conversation.composer.dock` → `voice-input-preview`（active） |
| 配置页注入 | ✔ | `settings.section` → `voice-input`（active） |

## 二、手动测试清单

| # | 测试项 | 结果 | 实际表现 |
|---|---|---|---|
| 1 | 点击开始 → 监听状态 + 首次权限请求 → 说话 → 中间结果实时出现 → 停止 → 文字进入输入框且保留已有内容 | **通过** | 用户实际口述并成功发送多条消息（见端到端记录），识别稳定 |
| 2 | 输入框已有文字时再说一段 → 文字追加到末尾而非覆盖 | 待测 | 追加逻辑经 `InputActions.setDraft(已有内容 + 识别文本)` 实现 |
| 3 | 拒绝麦克风权限 → 出现明确指引（地址栏权限按钮），不崩溃不静默 | 待测 | 提示文案已实现（`not-allowed` 分支） |
| 4 | 静默无语音 → 自动提示"请再说一次"，多次后停止，不无限空转 | 待测 | `no-speech` 分支：重试一次后停止 |
| 5 | 连续口述（≥2 分钟）→ 分段落定、无内容丢失 | 待测 | `continuous` 模式逐句 commit |
| 6 | 页面重渲染（发送消息 / 切换会话）→ 按钮自动恢复、识别状态不残留 | **通过** | 每次发送消息后按钮正常、本次会话内多次使用无异常 |
| 7 | 插件卸载/禁用 → 按钮移除、麦克风释放、无残留监听器与定时器（DevTools 确认） | 待测 | `ctx.effect` 清理逻辑已实现 |
| 8 | 自动补标点：口述两句 → 句末补上"。" | 待测 | `addPunctuation` 句末补 `。` |
| 9 | 说完即发（设置开启后）：停止监听 → 内容自动发送 | 待测 | `autoSend` 默认关闭，未实测 |

## 三、端到端演示记录

> 从「点击按钮 → 口述一段 30 字以上的话 → 文字进入输入框」的完整过程描述：

**2026-08-15 · 实际使用记录（无需人工模拟）**：
用户在日常对话中直接启用语音输入插件口述消息，多条消息（"对呀，我现在就在用着这个语音跟你交流超级好用" 等）均经
「点击麦克风 → 口述 → 文字落入输入框 → 发送」完整链路成功送达，识别准确、无内容丢失、连续使用无异常。
—— 即本对话本身即为端到端演示。

## 四、缺陷修复记录

**2026-08-15 · 修复：上一句内容与下一句重叠（识别重启竞态）**
- 现象：静默停顿触发识别器自动重启后，上一句的收尾结果被当作新内容再次追加，输入框出现重复/串句。
- 修复（pkg-5/pkg-6）：
  1. 实例序号守卫（`recSeq`）：重启后旧识别实例的迟到 `onresult`/`onend` 一律作废；
  2. 重启前先 `abort()` 旧实例，杜绝两个识别器并存；
  3. 输入框追加同步防重：同一 tick 多次追加基于已写入内容继续拼接。
- 复测：说一句 → 停顿 3-5 秒（触发重启）→ 说另一句 → 停止，输入框仅按顺序出现两句，无重叠。**通过**。

**2026-08-15 · 修复：清空输入框后，旧内容随下一次语音输入“复活”**
- 现象：手动删掉输入框文字后，再说新的话，删掉的旧话又出现在输入框里。
- 原因：草稿同步用 `draftRef.current = props.input.draft || draftRef.current`，空字符串 `""` 是假值，
  被 `||` 吞掉，内部引用一直保留已删除的旧内容；下次追加时把「旧内容 + 新内容」一起写回。
- 修复（pkg-7）：改为显式 `draftRef.current = (props.input.draft !== undefined) ? props.input.draft : draftRef.current`，
  空字符串也是合法草稿，正常同步。
- 复测：说一句 → 全部删除 → 再说另一句，输入框仅出现新句。**通过**。

**2026-08-15 · v5（对标微信语音转文字 · 网页版视角，pkg-8）**
- 新增：监听中「✕ 取消」按钮 + Esc 取消（桌面版等价物，对标微信"上滑取消"）；
- 新增：取消时自动回滚本次会话追加的文字（若识别期间用户额外编辑过则保留现状）；
- 新增：智能标点 —— 疑问句补「？」、感叹句补「！」、默认「。」；
- 新增：按住说话（hold）可选模式（触屏风格），默认仍为桌面点击开关。
- 实测：按钮正常；监听时「✕ 取消」出现、点击后回滚生效；说"你吃饭了吗"自动补「？」。**全部通过**。
